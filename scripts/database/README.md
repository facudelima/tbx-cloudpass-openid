# mongo db index scrips
All .js files shall be run in mongodb console

add one file per collections: `{collectionName}.js`

add on-time scrips and tools in  `oneTime/{refName}.{axorf}.js`

keep track of changes:

### last changes

*  (2024-07-12) created base db indexes
